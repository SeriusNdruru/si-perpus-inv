<img
    src="{{ asset('images/brand/site-logo.webp') }}?v=59"
    alt="Logo {{ $alt ?? ($systemBrand['institution.name'] ?? 'Instansi') }}"
    class="{{ $class ?? '' }}"
    width="{{ $width ?? 42 }}"
    height="{{ $height ?? 42 }}"
    decoding="async"
>
